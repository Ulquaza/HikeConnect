@inherits LayoutComponentBase

@inject HttpClient Http
@inject JwtAuthStateProvider Auth
@inject NavigationManager Nav

<div class="layout">
    <header tabindex="-1" class="header">
        <div class="header-container">
            <NavLink class="logo" href>HikeConnect</NavLink>

            <nav class="nav">
                <NavLink href="/about">О проекте</NavLink>
                <NavLink href="/contacts">Контакты</NavLink>
                <NavLink href="/trips">Походы</NavLink>
                <NavLink href="/create">Организовать</NavLink>
                @if (Auth.IsLoggedIn)
                {
                    <NavLink href="@("/user/{Auth.UserName}")">Профиль</NavLink>
                    <button class="link-button" @onclick="LogoutAsync">Выйти</button>
                }
                else
                {
                    <NavLink href="/login">Войти</NavLink>
                }
                
            </nav>
        </div>
    </header>

    <main class="content">
        @Body
    </main>
</div>

@code {
    private async Task LogoutAsync()
    {
        var response = await Http.GetAsync(ApiRoutes.Auth.Logout);
        if (response.IsSuccessStatusCode)
        {
            Auth.Logout();
            Nav.NavigateTo("/");
        }
    }
}